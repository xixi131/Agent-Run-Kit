#!/usr/bin/env sh
set -eu

echo "Customize init.sh for this project before running Auto_dev."
echo "Expected responsibilities:"
echo "1. install missing dependencies if needed"
echo "2. start the project runtime required for validation"
echo "3. fail fast when the environment cannot boot"
echo "This placeholder exits with failure until it is customized."
exit 1
